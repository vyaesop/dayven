name = "vertical-planner-api"
main = "src/index.ts"
compatibility_date = "2026-05-09"

[vars]
API_BEARER_TOKEN = ""

# Add the real Neon connection string to `.dev.vars` locally or to Worker secrets
# in Cloudflare. Example:
# NEON_DATABASE_URL=postgresql://user:password@ep-...neon.tech/dbname?sslmode=require
